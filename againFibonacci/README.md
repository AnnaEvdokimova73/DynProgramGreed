# Оценка сложности алгоритма

Сложность по памяти - константная О(1), поскольку используем дополнительно только локальные переменные num и i
Сложность по скорости - О(2^n), поскольку каждый вызов рекурсивной функции порождает еще 2 вызова (кроме индексов 0, 1, 2)
